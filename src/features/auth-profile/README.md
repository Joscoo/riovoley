# auth-profile

Feature clean-lite para resolución de perfil y rol del usuario autenticado.

- Fallbacks soportados: `user_profiles` -> `users` -> perfil base.
- Expone servicio para `useUserProfile`.
